{
  "extends": "solhint:recommended",
  "plugins": [],
  "rules": {
    "compiler-version": ["error", ">=0.8.20"],
    "avoid-suicide": "error",
    "avoid-sha3": "warn",
    "max-line-length": ["warn", 100]
  }
}
